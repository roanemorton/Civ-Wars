// Menu scene — handles win/lose/start screens
import Phaser from 'phaser';

export class MenuScene extends Phaser.Scene {
  constructor() {
    super('MenuScene');
  }

  create() {
    // Placeholder — menu UI will be built here
  }
}
